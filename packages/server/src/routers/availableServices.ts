import { Router } from 'express';
import { mapServiceRoutes } from '@asw-project/resources/routes';
// import { availableServiceKeys } from '../models/AvailableService';
// import { AvailableServiceService } from '../services/availableServices';

const router = Router();

// mapServiceRoutes(new AvailableServiceService(), availableServiceKeys)(router);

export default router;
